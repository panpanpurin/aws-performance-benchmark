{
  "name": "csv-processor-artillery",
  "version": "1.0.0",
  "private": true,
  "description": "Artillery helpers for CSV load tests. Run: npm install (only if form-data is missing). Artillery itself is launched via npx.",
  "main": "processor.js",
  "dependencies": {
    "form-data": "^4.0.4"
  }
}
