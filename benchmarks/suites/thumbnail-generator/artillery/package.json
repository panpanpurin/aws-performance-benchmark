{
  "name": "thumbnail-artillery",
  "version": "1.0.0",
  "private": true,
  "description": "Artillery helpers for thumbnail load tests. Run: npm install (only if form-data is missing). Artillery itself is launched via npx.",
  "main": "upload-image.js",
  "dependencies": {
    "form-data": "^4.0.4"
  }
}
