# Keep image build context small
node_modules
.git
.gitignore
.env
.env.*
*.log
*.md
npm-debug.log*
Dockerfile
Dockerfile.lambda
.dockerignore
# Excluded so a stray local lockfile cannot make a local build differ from a
# build made from a fresh clone, which has no lockfile.
package-lock.json
